# MultiModalSentimentAnalysis

From scratch PyTorch implementation of a Multimodal sentiment analysis deep learning model which predicts sentiment (positive, negative, neutral) from image + text pairs from MVSA-multiple (twitter) and TumEmo (tumblr) datasets.

Add things to the requirements.txt and setup.sh as you need for your env
